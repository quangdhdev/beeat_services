# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @beeat-be/apis`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t beeat-be-apis`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system beeat-be-apis && \
          adduser --system -G beeat-be-apis beeat-be-apis

COPY apps/apis/dist beeat-be-apis/
COPY apps/apis/package.json beeat-be-apis/
RUN chown -R beeat-be-apis:beeat-be-apis .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix beeat-be-apis --omit=dev -f install

CMD [ "node", "beeat-be-apis" ]
